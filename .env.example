# Squad investor wallet checkout (widget). Must start with VITE_ to be available in the app.
# Copy to `.env` or `.env.local` and paste your key. Restart `vite dev` after changing.
VITE_SQUAD_CHECKOUT_TOKEN=
